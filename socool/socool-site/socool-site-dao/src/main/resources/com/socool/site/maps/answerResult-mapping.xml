<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.socool.site.dao.answerresult.ITestAnswerResultMapper">
	<insert id="insertTestAnswerResult" parameterType="java.util.List" useGeneratedKeys="true" >
		insert into qdm20999941_db.test_answer_result(test_id,user_id,user_answer,status,create_time)values
		<foreach collection="list" item="item" index="index" separator=",">
			(#{item.testId},#{item.userId},#{item.userAnswer},#{item.status},UNIX_TIMESTAMP())
		</foreach>
	</insert>

</mapper>